lynx-pm (0.5.5-1) unstable; urgency=medium

  [FEATURES]
  * feat(start): --watch flag for auto-restart on file changes
  * feat(logs): daemon writes timestamps to log files
  * feat(logs): colored [STDOUT]/[STDERR] labels in lynx logs
  * feat(logs): gzip compression on log rotation (.log.N.gz)
  * feat(logs): O(n) ring buffer for log tail

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 17 Apr 2026 01:30:00 -0500

lynx-pm (0.5.4-1) unstable; urgency=medium

  * fix(debian): remove stray DEBHELPER token from postinst comment

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 17 Apr 2026 00:45:00 -0500

lynx-pm (0.5.3-1) unstable; urgency=high

  [SECURITY]
  * Fix shell command injection: quote arguments before sh -c concatenation
  * Block LD_*/DYLD_* env vars in system mode sanitization

  [FIXES]
  * Fix goroutine leak: cancel pending restart backoff on Stop()
  * Add Close() to audit logger, call on daemon shutdown
  * Log rotation errors now logged instead of silently swallowed
  * Kill stale user-mode daemons on .deb upgrade

  [UX]
  * Consistent ✓/✗/! symbols with colors across all CLI commands
  * All output respects --quiet flag via term.Printf
  * Update command shows exact wget + apt install for .deb users
  * Improved error messages: suggest 'lynx list' on process not found
  * Added examples to reload, restart, flush, apply commands
  * Fixed logs --help to render full options and examples

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 16 Apr 2026 19:00:00 -0500

lynx-pm (0.5.2-1) unstable; urgency=medium

  * deps: update all Go dependencies to latest versions

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 16 Apr 2026 18:30:00 -0500

lynx-pm (0.5.1-1) unstable; urgency=high

  [SECURITY]
  * security(H1): ed25519 release signing — 'lynx update --apply' now
    verifies a .sig companion asset before replacing the binary. Refuses
    unsigned releases unless --insecure-skip-signature is passed.
  * security(H2): unconditional PR_SET_NO_NEW_PRIVS in sandbox wrapper,
    closes setuid escape on kernels without landlock (< 5.13).
  * security(H3): reject absolute logs.stdout/stderr paths; add O_NOFOLLOW
    on log file open to prevent symlink-mediated arbitrary file writes.
  * security(H4): env_file ownership check — absolute env files must be
    owned by the calling UID (blocks cross-tenant secret exfiltration).
  * security(H5): abort sandbox if MS_PRIVATE mount fails — prevents host
    /proc unmount via shared mount propagation.
  * security(H6): per-sandbox private tmpfs over /tmp — isolates same-user
    sandboxes from planting binaries for each other.
  * security(M1): exact asset-name match in updater (was strings.Contains).
  * security(M2): broader package-system detection (dpkg+rpm+pacman, pre-
    and post-symlink paths).
  * security(M3): reject non-absolute cwd in sandbox config.
  * security(M4): refuse XDG_RUNTIME_DIR fallback to /tmp for socket path.
    XDG_RUNTIME_DIR now required; LYNX_SOCKET remains as override.

  [BREAKING CHANGES]
  * 'lynx update --apply' requires --insecure-skip-signature until the
    release workflow publishes signed assets.
  * User-mode daemon now requires XDG_RUNTIME_DIR to be set (standard on
    systemd-logind sessions). Non-login shells (cron, bare ssh) must
    export LYNX_SOCKET to an absolute path in a private directory.

  [FEATURES]
  * feat(cli): --isolation sandbox using user namespaces + landlock + rlimit
    (unprivileged container isolation, no sudo required).
  * feat(cli): global --quiet / -q flag.
  * feat(start): --dry-run / -n previews the resolved spec without contacting
    the daemon.
  * feat(cli): lynx completion <bash|zsh|fish> emits shell completion scripts.
  * feat(install-tools): default to ~/.local/bin, add --system flag for the
    /usr/local/bin path (no longer requires root in the common case).
  * feat(cli): add 'reset' command to zero Restarts counter without restart.
  * feat(cli): add 'scale' command to grow/shrink an app post-hoc.
  * feat(list): add --json flag for machine-readable output.
  * feat(ipc): per-UID token-bucket rate limit on every IPC request (ON by
    default, 200 burst / 100 req/s per UID).
  * feat(daemon): audit log (JSON-lines) for every destructive action.
    ON in system mode to /var/log/lynx-pm/audit.log.
  * feat(start): configurable stop signal (--stop-signal) and graceful
    timeout (--stop-timeout, default 10s).
  * feat(start): resource limits via --memory-max, --cpu-max, --tasks-max.
    Enforced via cgroup v2 (dynamic) or rlimit (sandbox).
  * feat(daemon): size-based log rotation (50 MiB default, 3 backups).
  * ci: GitHub Actions release workflow with ed25519 binary signing.

  [FIXES]
  * fix(daemon): detect system mode by uid 0 OR lynx user. --isolation
    dynamic now works on the shipped .deb (previously rejected because
    lynxd runs as the lynx user, uid != 0).
  * fix(handlers): validate cwd is accessible to the daemon user; replaces
    the cryptic "fork/exec: permission denied" with a clear error.
  * fix(polkit): grant manage-units when the "unit" detail is absent, which
    is the typical case for systemd-run's StartTransientUnit. Unblocks
    --isolation dynamic on the shipped .deb.
  * fix(process): count every restart regardless of trigger (cron ticks
    were not incrementing Restarts).
  * fix(manager): reject duplicate (namespace, name) on start with ERR_CONFLICT.
  * fix(cli): open IPC client lazily — bad invocations no longer emit a
    daemon connection error before the usage message.
  * fix(ipc): replace cryptic dial error with actionable daemon-off hint.
  * docs: add SECURITY.md, ARCHITECTURE.md, runnable examples in --help.

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 16 Apr 2026 18:00:00 -0500

lynx-pm (0.4.10-1) unstable; urgency=medium

  * fix(ipc): allow lynxadm users to run user-mode daemons if system socket is missing

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 12 Mar 2026 01:10:00 -0500

lynx-pm (0.4.9-1) unstable; urgency=medium

  * feat(cli): implement lynx startup for non-root users (systemd --user)

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 11 Mar 2026 04:30:00 -0500

lynx-pm (0.4.8-1) unstable; urgency=medium

  * fix(cli): improve install-tools path detection for sudo users
  * fix(cli): prevent symlink loops in install-tools

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 11 Mar 2026 04:05:00 -0500

lynx-pm (0.4.7-1) unstable; urgency=medium

  * perf(metrics): optimize linux process tree scanning and remove string overhead

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 10 Mar 2026 21:38:00 -0500

lynx-pm (0.4.6-1) unstable; urgency=medium

  * feat(daemon): allow colon character in process names
  * fix(debian): migrate lynx user home directory in postinst

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 10 Mar 2026 20:30:00 -0500

lynx-pm (0.4.5-1) unstable; urgency=medium

  * feat(env): implement custom robust env parser to handle quotes and comments
  * feat(cli): change default logs mode to "file" in start command
  * feat(cli): allow spaces in process names
  * fix(cli): add 'log' alias to 'logs' command

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 10 Mar 2026 03:00:00 -0500

lynx-pm (0.4.4-1) unstable; urgency=medium

  * feat(cli): allow selective tool installation in install-tools

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 10 Mar 2026 02:00:00 -0500

lynx-pm (0.4.3-1) unstable; urgency=medium

  * refactor: replace all occurrences of UUID v4 with UUID v7

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 10 Mar 2026 01:13:00 -0500

lynx-pm (0.4.2-1) unstable; urgency=medium

  * fix(updater): fix Chmod called on closed file descriptor
  * fix(updater): add timeout to download HTTP client
  * fix(updater): add io.LimitReader to prevent disk exhaustion
  * fix(updater): implement semver comparison to prevent downgrades
  * perf(daemon): eliminate double mutex lock in restart handler
  * perf(daemon): use map for O(1) env whitelist lookup
  * refactor(manager): remove goto in ResolveID
  * fix(paths): remove dead-code duplicate check in withinRoot

 -- Jaro-c <jaro-c@users.noreply.github.com>  Sat, 08 Mar 2026 20:30:00 -0500

lynx-pm (0.4.1-1) unstable; urgency=medium

  * feat(git): add support for git metadata detection in managed applications
  * feat(cli): display git branch, commit hash, and dirty status in lynx list
  * feat(daemon): capture git metadata at process startup time
  * docs(list): update documentation to include new git information

 -- Jaro-c <jaro-c@users.noreply.github.com>  Sat, 08 Mar 2026 12:00:00 +0000

lynx-pm (0.4.0-1) unstable; urgency=medium

  * Fix(cli): use correct systemd service name 'lynx.lynxd.service' in startup command
  * Bump version to 0.4.0

 -- Jaro-c <jaro-c@users.noreply.github.com>  Fri, 27 Feb 2026 13:00:00 +0000

lynx-pm (0.3.9-1) unstable; urgency=medium

  * Fix systemd service name mismatch (lynx.lynxd.service)
  * Ensure systemd service is correctly installed by debhelper

 -- Jaro-c <jaro-c@users.noreply.github.com>  Fri, 27 Feb 2026 12:30:00 +0000

lynx-pm (0.3.8-1) unstable; urgency=medium

  * chore: rename system paths to /var/lib/lynx-pm and /var/log/lynx-pm
  * fix: avoid conflicts with lynx browser package

 -- Jaro-c <jaro-c@users.noreply.github.com>  Fri, 27 Feb 2026 12:00:00 +0000

lynx-pm (0.3.7-1) unstable; urgency=medium

  * feat(isolation): enhance dynamic mode with ProtectProc=invisible
  * fix(daemon): correctly detect privileged mode for handlers
  * fix(ipc): allow non-root users in admin group to connect to daemon
  * fix(build): disable git vcs stamping during debian rules compilation
  * docs: update flag documentation

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 27 Feb 2026 10:00:00 +0000

lynx (0.3.5-1) unstable; urgency=medium

  * Fix CLI client socket resolution. Users within the 'lynxadm' group will now successfully connect to the System Daemon (/run/lynxd/lynx.sock) by default instead of falling back to isolated user daemons.

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 25 Feb 2026 20:55:00 +0000

lynx (0.3.4-1) unstable; urgency=medium

  * Fix IPC socket ownership validation to permit daemon execute user instead of strictly requiring root

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 25 Feb 2026 19:08:04 +0000

lynx (0.3.3-1) unstable; urgency=medium

  * Fix systemd RuntimeDirectory not mounting /run/lynxd correctly

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 25 Feb 2026 14:30:37 +0000

lynx (0.3.2-1) unstable; urgency=medium

  * Fix IPC socket directory ownership conflicts by renaming to /run/lynxd

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 25 Feb 2026 14:11:37 +0000

lynx (0.3.1-1) unstable; urgency=medium

  * Update CLI instructions for deb installations

 -- Jaro-c <jaro-c@users.noreply.github.com>  Wed, 25 Feb 2026 12:26:18 +0000

lynx (0.3.0-1) unstable; urgency=medium

New release 0.3.0  * 

 -- Admin <jaroc@Admin.localdomain>  Tue, 24 Feb 2026 21:54:38 -0500

lynx (0.3.0-1) unstable; urgency=medium

  * Add self-updater with GitHub releases integration
  * Add logs command with PM2-like output and log following
  * Introduce namespace support for application isolation
  * Add logrotate configuration integration
  * Add GitHub Sponsors funding configuration
  * Enhance security, ID validations, and system socket permissions
  * Improve Linux tests and adopt go 1.26.0

 -- Jaro-c <jaro-c@users.noreply.github.com>  Tue, 24 Feb 2026 21:37:42 -0500

lynx (0.1.2-1) unstable; urgency=medium

  * Add PM2-like flags for start command
  * Implement restart policy and log file management
  * Standardize command documentation

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 29 Jan 2026 12:00:00 +0000

lynx (0.1.1-1) unstable; urgency=medium

  * Fix metrics and Linux-only consistency
  * Improve CLI ID display and documentation

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 29 Jan 2026 12:00:00 +0000

lynx (0.1.0-1) unstable; urgency=medium

  * Switch JSON backend to bytedance/sonic
  * Use google/uuid for AppSpec IDs
  * Improve Debian packaging and systemd unit
  * Allow root to manage system daemon

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 29 Jan 2026 12:00:00 +0000

lynx (0.0.1-1) unstable; urgency=medium

  * Initial release.

 -- Jaro-c <jaro-c@users.noreply.github.com>  Thu, 29 Jan 2026 12:00:00 +0000
